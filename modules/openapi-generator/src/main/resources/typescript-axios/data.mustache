import * as fs from 'fs';

export const TestData : { [key: string]: any }= {
{{#operations}}
{{#operation}}
    "{{{operationId}}}" : {
        "scenarios" : {
            {{#scenarios}}
            "{{-index}}": {
                "isValid": {{valid}},
                "summary" : "{{{scenario}}}",
                "parameters" : {
                    {{#parameters.0}}
                    {{#parameters}}
                    "{{{baseName}}}" : {{{example}}}, 
                    {{/parameters}}
                    {{/parameters.0}}
                },
            },
        {{/scenarios}}
        }
    },
{{/operation}}
{{/operations}}
}